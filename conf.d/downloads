#!/bin/bash -ex

dl() {
    [ "$FAB_HTTP_PROXY" ] && PROXY="--proxy $FAB_HTTP_PROXY"
    cd $2; curl -L -f -O $PROXY $1; cd -
}

VERSION="2-0-4"
URL="http://mirror.ord.simplemachines.org/downloads/smf_${VERSION}_install.tar.gz"

dl $URL /usr/local/src

